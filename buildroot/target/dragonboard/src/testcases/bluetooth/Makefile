
# define sources root directory before everything
SRC_ROOT := ../..

# include rule.mk
include $(SRC_ROOT)/rule.mk

.PHONY: all
all:
	cp bttester.sh $(BINDIR)/
	cp rt_bttester.sh $(BINDIR)/	
	cp hcitool $(BINDIR)/
	cp cciconfig $(BINDIR)/
	cp bluetooltester $(BINDIR)/
	cp *.hcd $(BINDIR)/
	cp rtl8723b_config $(BINDIR)/
	cp rtl8723b_fw $(BINDIR)/